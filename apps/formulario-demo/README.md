# Formulario demo (solo frontend)

Registro de **nombre**, **correo** y **celular**. Validación en el navegador; **no hay backend** ni envío de datos.

## Abrir en local

Desde esta carpeta:

```powershell
# Opción 1 — abrir archivo
start index.html

# Opción 2 — servidor simple (recomendado para demo)
py -m http.server 8080
```

Luego visita: http://localhost:8080

## Contexto del equipo

Ver `team-brain/projects/formulario-demo/overview.md` y palabra clave `formulario` en KEYWORDS.
